import pytest


def test_perf_sync_incremental_small(tmp_path):
    """Performance smoke test placeholder for incremental sync (synthetic dataset)."""
    pytest.fail("Perf test placeholder — failing by design to enforce TDD")
